import styled from '@emotion/styled';

const TodoWebContainer = styled.section`
  padding: 35px 30px 63px;
  border-radius: 8px;
  box-shadow: 0 2px 16px 0 var(--black-10);
  background-color: #fff;
`;

export default TodoWebContainer;
